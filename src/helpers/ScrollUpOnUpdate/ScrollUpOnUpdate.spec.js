import ScrollUpOnUpdate from "./ScrollUpOnUpdate"

describe("<ScrollUpOnUpdate />", () => {
  it("can exist", () => {
    const wrapper = mount(<ScrollUpOnUpdate />)
    expect(wrapper).to.exist
  })
})
