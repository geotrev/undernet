#### Webpack

```js
// Get Undernet from /node_modules/
// If you aren't using npm, "undernet" should be an absolute path
import Undernet from "undernet"
var start = Undernet.start()
document.addEventListener('DOMContentLoaded', start)
```
